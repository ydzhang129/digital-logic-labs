# 使用说明

- 开发板型号：xc7a100tcsg324

验收试验时，将需要启用的设计源文件设置为“顶层模块”，并启用对应的约束文件，设置方法：

1. 右键需要启用的设计源文件(Design Sources)
2. 点击“设置为‘顶层模块’”(Set as Top)
3. **将无关约束文件设置为“不可用”(右键-Disable File)**，将需要启用的约束文件设置为“可用”(右键-Enable File)
